#include <iostream>
#include "../../DataStructure/BinaryTree.h"

class Solution {
public:
    bool isBalanced(TreeNode* root) {
        if (!root) {return true;}
        
    }
};

int main(int argc, const char** argv) {
    return 0;
} 